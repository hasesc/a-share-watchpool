# 统一换行符处理
* text=auto

# Python 脚本强制 LF
*.py text eol=lf

# PowerShell 脚本强制 CRLF（Windows 兼容）
*.ps1 text eol=crlf

# Markdown 和 JSON 使用 LF
*.md text eol=lf
*.json text eol=lf

# 二进制文件不转换
*.sqlite binary
*.png binary
*.jpg binary
